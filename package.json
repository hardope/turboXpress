{
  "name": "turbo-xpress",
  "version": "1.0.0",
  "description": "A package to initialize a basic Express.js project with turbo speed",
  "main": "dist/index.js",
  "bin": "./bin/turbo.js",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Opeoluwa Adeyeri",
  "license": "MIT",
  "dependencies": {
    "colors": "^1.4.0",
    "fs-extra": "^11.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist",
    "bin",
    "LICENSE",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hardope/turboXpress.git"
  }
}
